import "time"

// Note: Time is CET by default
is_weekday = rule { time.day not in ["saturday", "sunday"] }
is_deployment_hours = rule { time.hour > 8 and time.hour < 17 }

main = rule {
	is_deployment_hours and is_weekday
}
